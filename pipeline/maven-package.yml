platform: linux

image_resource:
  type: docker-image
  source:
    repository: https://github.com/kalaiganeshan/concourse
    tag: latest

inputs:
  - name: concourse

#outputs:
#  - name: jar-file
#    path: git-repo/target/

run:
  path: mvn
  args: ["-f", "concourse/pom.xml", "-DskipTests=true", "package"]

